/**
 * CREDENTIALS TYPE TEMPLATE
 *
 * This file defines the credential type for your integration.
 * It is shared between the app step and the auto-generated export code.
 *
 * TODO:
 * 1. Update the type name to match your integration (e.g., MyIntegrationCredentials)
 * 2. Add all credential fields your integration needs (API keys, tokens, etc.)
 */

export type [IntegrationName]Credentials = {
  [INTEGRATION_NAME]_API_KEY?: string;
  // Add other credential fields as needed (e.g., workspace ID, region, etc.)
};

